[Unit]
Description=Sets a random startup video on startup

[Service]
Type=oneshot
WorkingDirectory=WORKDIR
ExecStart=/bin/bash SCRIPTDIR

[Install]
WantedBy=default.target
WantedBy=graphical-session.target
